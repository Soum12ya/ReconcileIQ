# app/tools/ocr_tool.py
def extract_text_async(file_path: str, case_id: str):
    # placeholder — in Phase 2 you'd push this to its own job queue,
    # not call it inline in the /invoices request handler
    return "stub: OCR job queued"